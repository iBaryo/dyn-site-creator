import {FeatureComponent} from "./FeatureComponent";

xdescribe('FeatureComponent', () => {
    // todo: specs

    it('should disable previous validations', () => {
    });
    describe('install', () => {
        it('should install backend nodes', () => {
        });
        it('should not install frontend nodes (as not all backend html page nodes are available)', () => {
        });
    });
    describe('activator', () => {
        it('should install frontend components', () => {
        });
        it('should activate all backend nodes', () => {
        });
        it('should resolve only when all backend nodes were activated', () => {
        });
    });
});