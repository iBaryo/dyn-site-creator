xdescribe('EndpointComponent', () => {
    describe('install', () => {
        it('should throw if missing `name` in code node', () => {
        });
        it('should still use super\'s validation', () => {
        });
        describe('activation object', () => {
            it('should call and return super\'s `run`', () => {
            });
            it('should define and endpoint according to node code\'s name', () => {
            });
            it('should invoke function for a request', () => {
            });
            it('should swallow exception if the function throws', () => {
            });
        });
    });
});