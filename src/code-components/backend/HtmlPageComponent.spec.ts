xdescribe('HtmlPageComponent', () => {
    it('should call and return super\'s `run`', () => {
    });
    it('should throw if code node has on name', () => {
    });
    it('should allow code node with no code (using default html template)', () => {
    });
    it('should install all frontend components', () => {
    });
    describe('activation object', () => {
        it('should activate all frontend components per request', () => {
        });
        it('should invoke input function for each request and respond with its result', () => {
        });
        it('should invoke input function with the result of activating all frontend components', () => {
        });
        it('should respond with the error if the input function throws', () => {
        });
        it('should respond with the error if the input function\'s resulted promise rejects', () => {
        });
        it('should always respond with contentType of application/html', () => {
        });
    });
});