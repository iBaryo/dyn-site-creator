xdescribe('JsonEndpointComponent', () => {
    it('should call and return super\'s `run`', () => {
    });

    describe('activator', () => {
        it('should invoke the input function with the request and config', () => {
        });
        it('should should response with the input function\'s result', () => {
        });
        it('should wait for returned promise and respond with its value', () => {
        });
        it('should always respond in application/json format', () => {
        });
        it('should respond with error if input function throws', () => {
        });
        it('should respond with error if input function\'s returned promise rejects', ()=> {});
    });
});