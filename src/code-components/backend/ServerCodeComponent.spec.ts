xdescribe('ServerCodeComponent', () => {
    describe('activation object', () => {
        it('should invoke the input function with the context\'s app and config', ()=> {});
        it('should return the input function\'s value', ()=> {});
        it('should throw if the input function throws', ()=> {});
        it('should throw if the input function\'s returned promise rejects', ()=> {});
    });
});