describe('CustomScriptComponent', () => {
    it('should only allow a function in the code node', () => {
    });
    it('should return an IIFE string', () => {
    });
    it('should invoke IIFE (in the browser) with config on window', () => {
    });
    it('should be extendable so more args can be injected to the input function scope', () => {
    });
});