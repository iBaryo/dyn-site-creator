xdescribe('DomComponent', () => {
    it('should invoke input function with the request and config', () => {
    });
    it('should unwrap the input function returned promise and return it', () => {
    });
    it('should return an HTML comment with the error if throws', () => {
    });
    it('should do the above when input function\'s returned promise rejects', () => {
    });
});