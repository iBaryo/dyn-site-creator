xdescribe('FrontendCodeComponent', () => {
    it('should allow code node with no code', () => {
    });
    it('should allow code that is not a function', () => {
    });
    describe('activator', () => {
        it('should invoke `run` method with the code node, input function and the request object', () => {
        });
        it('should return the code node\'s code as the input function (if the code is not a function)', () => {
        });
        it('(the input function) should return an empty string if code node has no code', () => {
        });
    });
});