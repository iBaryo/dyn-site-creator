import {FrontendCodeExecutor} from "./FrontendCodeExecutor";
import {CodeNode} from "../../ConfigurationTypes";

// export class DomExecuter extends FrontendCodeExecutor {
//     protected async setupFunction(fn: (req,config)=>Promise<string>, options: CodeNode) {
//         return await fn()
//     }
//
// }